# We don't want to track the .dvi files created by the 'latex' command
*.dvi

# Nor do we want to track the .pdf files created by pdflatex
*.pdf

# On issuing the 'latex' command, several auxiliary files are generated
# We don't want to track these either
*.aux
*.log
*.out

